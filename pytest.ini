[pytest]
addopts = -q --cov=app --cov=core --cov-report=term-missing:skip-covered --cov-fail-under=85
asyncio_mode = auto
testpaths = tests
filterwarnings =
    error::DeprecationWarning
    ignore:pkg_resources is deprecated as an API:DeprecationWarning
